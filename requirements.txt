pdfplumber==0.11.9
# Both arrive transitively via pdfplumber, but extract/ocr.py imports them
# DIRECTLY to render a page for OCR -- pypdfium2 because it is a
# self-contained wheel (the Render build is pip-only and cannot install the
# ImageMagick/Ghostscript that page.to_image() shells out to), and Pillow to
# encode the bitmap as PNG. A direct import on an undeclared transitive dep
# breaks silently the day pdfplumber drops it.
pypdfium2==5.12.1
Pillow==12.3.0
# extract/vision.py -- the OCR transcriber. Imported LAZILY (inside
# ClaudeTranscriber.client), so a deployment with CIM_OCR_ENABLED unset never
# constructs a client or reads a key; the pin is here because CI installs this
# file and tests/test_vision_transcriber.py must import the module.
anthropic==0.122.0
python-docx==1.2.0
openpyxl==3.1.5
numpy-financial==1.0.0
requests==2.33.0
beautifulsoup4==4.14.3

# ── Django front end (pins mirrored from managertools) ──
Django==5.1.15
django-allauth==65.16.1
django-environ==0.13.0
django-htmx==1.27.0
gunicorn==25.3.0
whitenoise==6.12.0
psycopg==3.3.4
psycopg-binary==3.3.4
pytailwindcss==0.3.0
pytest==9.0.3
pytest-django==4.12.0
oauthlib==3.3.1
PyJWT==2.12.1
